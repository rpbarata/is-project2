<div class="row">
  <div class="col">
    <div class="d-inline-block">
      <p class="text"> <span class="fw-bold">Departure Point: </span> <%= @trip.departure_point.name %></p>
    </div>
  </div>
</div>

<div class="row">
  <div class="col">
    <div class="d-inline-block">
      <p class="text"> <span class="fw-bold">Destination: </span> <%= @trip.destination_point.name %></p>
    </div>
  </div>
</div>

<div class="row">
  <div class="col">
    <div class="d-inline-block">
      <p class="text"> <span class="fw-bold">Departure Time: </span> <%= @trip.departure_time %></p>
    </div>
  </div>
</div>

<div class="row">
  <div class="col">
    <div class="d-inline-block">
      <p class="text"> <span class="fw-bold">Capacity: </span> <%= format_trip_capacity(@trip) %></p>
    </div>
  </div>
</div>

<div class="row">
  <div class="col">
    <div class="d-inline-block">
      <p class="text"> <span class="fw-bold">Price: </span> <%= number_to_currency(@trip.price, :unit => "€")%></p>
    </div>
  </div>
</div>

<h1 class="mt-3">Passengers</h1>
<div class="table-responsive">
  <table class="table table-sm table-striped table-hover align-middle">
    <thead>
      <tr>
        <th scope="col" >#</th>
        <th scope="col">Email</th>
        <th scope="col">Name</th>
      </tr>
    </thead>

    <tbody>
      <% @passengers.each_with_index do |passenger, i| %>
        <tr>
          <th scope="row"><%= (i+1) %></th>
          <td><%= passenger.email %></td>
          <td><%= passenger.username %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<%= paginate @passengers %>
